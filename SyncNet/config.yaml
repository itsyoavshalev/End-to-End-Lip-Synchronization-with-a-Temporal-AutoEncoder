train:
  batch_size: 16
  num_epochs: 100
  max_eval_iterations: 50
  lr: 0.0001
  num_workers: 5
test:
  batch_size: 8
  num_workers: 0
general:
  frames_shift_window_train: 50
  frames_search_window_test: 25 # should match the values generated by test_db_shifts_file_generator.py
  ef_frames_window_size: 5
  audio_window_size: 20
  gpu_ids: '1' # for example '0 1'
  output_path: './SyncNet/outputs'
  logs_folder: 'logs'
  checkpoints_folder: 'checkpoints'
  current_state_file_name: 'current_state.txt'
  eval_epcohs_freq: 1
